!function ($, jQuery) {
    "use strict";

    /**
     * @constructor
     * @public
     *
     * @param $scope
     */
    function resourcesController($scope ) {


        var init = function(){

        };

        init();
    }

    app.module.controller('resourcesController', resourcesController);
}();
