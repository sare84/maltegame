!function ($, jQuery) {
    "use strict";

    /**
     * @constructoru
     * @public
     *
     * @param $scope
     */
    function aboutController($scope) {
        var init = function(){ 
        }
        init();
    }
    app.module.controller('aboutController', aboutController);
}();
