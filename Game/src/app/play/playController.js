!function ($, jQuery) {
    "use strict";

    /**
     * @constructoru
     * @public
     *
     * @param $scope
     */
    function playController($scope) {
        var init = function(){
        }

        init();
    }
    app.module.controller('playController', playController);
}();
